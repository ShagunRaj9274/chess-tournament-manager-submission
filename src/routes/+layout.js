// The database lives in the browser (SQLite compiled to WebAssembly), so the
// app renders entirely on the client and ships as a static bundle.
export const ssr = false;
export const prerender = false;
